version: '2.2'
services:
  Allen:
    image: grpcnew
    ports:
      - "4000:4000"
    networks:
      - proxynet
    environment:
      - ADD_PORT=4000
      - FRND_PORT=5000
      - USER=Allen
      - TIME=1000  
  Bob:
    image: grpcnew
    ports:
      - "5000:5000"
    networks:
      - proxynet
    environment:
      - ADD_PORT=5000
      - FRND_PORT=4000
      - USER=Bob 
      - TIME=2000    
networks:
  proxynet:
    driver: bridge